function multiplicar(a: number, b: number): number {
  return a * b;
}

function saudar(name: string): string {
  return `Olá ${name}`;
}

console.log(multiplicar(2, 3)); 
console.log(saudar("Thayná")); 